<% provide(:title, 'Registro') %>
<% provide(:button_text, 'Crear cuenta') %>
<h1>Registro</h1>

<div class="row">
  <div class="col-sm-4 offset-sm-4 form-group" id="error_explanation">
    <%= render 'form' %>
  </div>
</div>
<!--
  <div class="col-sm-4 form-group">
    <%= form_for(@user, url: registro_path) do |f| %>

      <%= f.label :name, "Nombre" %>
      <%= f.text_field :name, class: 'form-control'  %>

      <%= f.label :email %>
      <%= f.email_field :email, class: 'form-control', placeholder: "ejemplo@ejemplo.com"  %>

      <%= f.label :password, "Contraseña (Al menos 6 caracteres)" %>
      <%= f.password_field :password, class: 'form-control'  %>

      <%= f.label :password_confirmation, "Repetir contraseña" %>
      <%= f.password_field :password_confirmation, class: 'form-control'  %>

      <%= f.submit "Crear mi cuenta", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
-->
